from sqlalchemy import (
    Column,
    String,
    DateTime,
    Integer,
)
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.sql import func


base = declarative_base()


class CourseClassPost(base):

    __tablename__ = "course_class_posts"

    id = Column(Integer, primarys_key=True)
    course_class_id = Column(Integer)
    title = Column(String)
    body = Column(String)
    created_at = Column(DateTime(timezone=False), default=func.now())
    updated_at = Column(DateTime(timezone=False), default=func.now())
