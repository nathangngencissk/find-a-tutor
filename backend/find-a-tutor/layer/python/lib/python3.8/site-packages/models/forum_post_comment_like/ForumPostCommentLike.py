from sqlalchemy import (
    Column,
    String,
    DateTime,
    Integer,
    Boolean,
)
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.sql import func


base = declarative_base()


class ForumPostCommentLike(base):

    __tablename__ = "forum_posts_comments_likes"

    id = Column(Integer, primary_key=True)
    user_id = Column(String)
    post_comment_id = Column(Integer)
    value = Column(Boolean)
    created_at = Column(DateTime(timezone=False), default=func.now())
    updated_at = Column(DateTime(timezone=False), default=func.now())
